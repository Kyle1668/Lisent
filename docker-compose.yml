version: "3"
services:
  api:
    build: .
    restart: always
    ports:
      - '5000:5000'
  nginx:
    image: 'nginx:alpine'
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - './nginx.conf:/etc/nginx/conf.d/default.conf'
    links:
      - api
